{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(git *)",
      "Bash(sudo systemctl *)",
      "Bash(sudo sed -i * /etc/systemd/system/*)",
      "Bash(sudo journalctl *)",
      "Bash(dig *)",
      "Bash(curl *)"
    ]
  }
}
