# Homelab — Claude Project Notes

Global Claude context for this machine lives in `~/.claude/CLAUDE.md`.

This file is kept in the repo so other machines (e.g. Windows PC) have a pointer when cloning.
Project-specific docs are in `services/` and `network/`.
